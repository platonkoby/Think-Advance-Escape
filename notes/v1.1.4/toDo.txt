step one Todos:

    - create an array of construction titles, which will be used to create
    constructions and also be a part of tags
    - create delayed actions for constructions
    - assign construction actions to constructions

step two Todos:

    - update setWaiting list, so that if action has a name of the construction
    in its forTags, and if that construction was built. stages initalActions,
    were moved to waititng, and inital are those inside the construction actions
    list

